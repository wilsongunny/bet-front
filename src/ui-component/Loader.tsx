import LinearProgress from '@mui/material/LinearProgress';
import { LoaderWrapper } from 'ui-component';

const Loader = () => (
    <LoaderWrapper>
        <LinearProgress color="primary" />
    </LoaderWrapper>
);

export default Loader;
